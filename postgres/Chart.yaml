apiVersion: v2
dependencies:
- name: common
  repository: oci://registry-1.docker.io/wodby
  version: 1.x.x
name: postgres
version: 0.3.1
annotations:
  artifacthub.io/changes: |
    - kind: fixed
      description: Omit RollingUpdate settings when the OnDelete StatefulSet strategy is selected
