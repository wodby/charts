apiVersion: v2
appVersion: 4.0.8
name: nfs-provisioner
version: 0.4.1
annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Declare the standard nameOverride and fullnameOverride chart values
dependencies:
- name: common
  repository: oci://registry-1.docker.io/wodby
  version: 1.x.x
