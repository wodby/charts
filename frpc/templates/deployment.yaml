apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 6 }}
  template:
    metadata:
      labels: {{- include "common.labels.standard" ( dict "customLabels" $podLabels "context" $ ) | nindent 8 }}
      {{- if .Values.podAnnotations }}
      annotations:
        {{- include "common.tplvalues.render" ( dict "value" .Values.podAnnotations "context" $) | nindent 8 }}
      {{- end }}
    spec:
      {{- include "frpc.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ template "frpc.serviceAccountName" . }}
      automountServiceAccountToken: false
      containers:
        - name: frpc
          image: {{ include "frpc.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
          args:
            - frpc
            - -c
            - /etc/frpc.toml
          {{- if .Values.frpc.egressProxyExistingSecret }}
          envFrom:
            - secretRef:
                name: {{ .Values.frpc.egressProxyExistingSecret | quote }}
                optional: true
          {{- end }}
          env:
            - name: FRPC_NAME
              value: {{ .Values.frpc.name | quote }}
            - name: FRPC_SERVER_ADDR
              value: {{ .Values.frpc.serverAddr | quote }}
            - name: FRPC_SERVER_PORT
              value: {{ .Values.frpc.serverPort | quote }}
            - name: FRPC_AUTH_METHOD
              value: 'oidc'
            - name: FRPC_LOCATION
              value: {{ .Values.frpc.location | quote }}
            - name: FRPC_LOCAL_PORT
              value: '8001'
            # FRPS consumes HTTP Basic auth; kubectl proxy must receive an empty
            # Authorization header so client-go can add its rotating token.
            - name: FRPC_CLEAR_AUTHORIZATION_HEADER
              value: '1'
            - name: FRPC_HTTP_USER
              value: {{ .Values.frpc.httpUser | quote }}
            - name: FRPC_HTTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "frpc.secretName" . }}
                  key: FRPC_HTTP_PASSWORD
            - name: FRPC_AUTH_OIDC_CLIENT_ID
              value: {{ .Values.frpc.oidcClientID | quote }}
            - name: FRPC_AUTH_OIDC_TOKEN_ENDPOINT_URL
              value: {{ .Values.frpc.oidcTokenEndpointURL | quote }}
            - name: FRPC_CUSTOM_DOMAIN
              value: {{ .Values.frpc.customDomain | quote }}
            - name: FRPC_AUTH_OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "frpc.secretName" . }}
                  key: FRPC_AUTH_OIDC_CLIENT_SECRET
            {{- with .Values.envVars }}
            {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 12 }}
            {{- end }}
          {{- if .Values.extraVolumeMounts }}
          volumeMounts:
            {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumeMounts "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.resources }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          {{- end }}
        - name: proxy
          image: {{ include "frpc.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
          # Keep the cluster-admin token out of the FRPC container.
          volumeMounts:
            - name: kube-api-access
              mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              readOnly: true
          env:
            - name: KUBECTL_API_PREFIX
              value: {{ .Values.frpc.apiPrefix | quote }}
          command:
            - kubectl
            - proxy
            - --accept-hosts=.*
            - --disable-filter
            - --address=127.0.0.1
            - --api-prefix=$(KUBECTL_API_PREFIX)
          {{- if .Values.resources }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          {{- end }}
      volumes:
        - name: kube-api-access
          projected:
            defaultMode: 420
            sources:
              - serviceAccountToken:
                  path: token
                  expirationSeconds: 3600
              - configMap:
                  name: kube-root-ca.crt
                  items:
                    - key: ca.crt
                      path: ca.crt
              - downwardAPI:
                  items:
                    - path: namespace
                      fieldRef:
                        apiVersion: v1
                        fieldPath: metadata.namespace
        {{- if .Values.extraVolumes }}
        {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumes "context" $) | nindent 8 }}
        {{- end }}
      {{- if .Values.affinity }}
      affinity: {{- include "common.tplvalues.render" (dict "value" .Values.affinity "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" (dict "value" .Values.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.tolerations "context" $) | nindent 8 }}
      {{- end }}
