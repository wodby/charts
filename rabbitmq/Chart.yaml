apiVersion: v2
name: rabbitmq
description: RabbitMQ chart for Wodby RabbitMQ images.
type: application
version: 0.3.1
appVersion: 4.3.4
keywords:
  - rabbitmq
dependencies:
  - name: common
    repository: oci://registry-1.docker.io/wodby
    version: 1.x.x
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: Wait for safe quorum state and drain RabbitMQ before pod termination
    - kind: changed
      description: Increase the default termination grace period to 270 seconds
