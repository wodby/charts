apiVersion: v2
name: supabase
description: Self-hosted Supabase APIs and Studio with an external Supabase PostgreSQL database
type: application
version: 0.1.1
appVersion: self-hosted/v0.8.1
annotations:
  artifacthub.io/changes: |
    - kind: fixed
      description: Declare signing and verification key operations so Auth starts successfully
