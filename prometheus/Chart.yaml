apiVersion: v2
name: prometheus
description: Prometheus server chart for Wodby services
type: application
version: 0.2.1
annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Drain service endpoints before Prometheus shuts down
appVersion: "3.13.0"
dependencies:
- name: common
  repository: oci://registry-1.docker.io/wodby
  version: 1.x.x
