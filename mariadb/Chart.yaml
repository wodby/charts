annotations:
  category: Database
  licenses: Apache-2.0
  artifacthub.io/changes: |
    - kind: fixed
      description: Avoid duplicate WODBY environment variables when platform variables are injected
apiVersion: v2
dependencies:
- name: common
  repository: oci://registry-1.docker.io/wodby
  version: 1.x.x
name: mariadb
version: 0.2.2
