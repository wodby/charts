apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-agent.fullname" . }}-test-connection"
  labels:
    {{- include "node-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
