apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-agent.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
