annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Stop accepting traffic and finish active NGINX requests during pod termination
  category: Infrastructure
  licenses: Apache-2.0
apiVersion: v2
dependencies:
- name: common
  repository: oci://registry-1.docker.io/wodby
  version: 1.x.x
name: nginx
version: 0.3.1
