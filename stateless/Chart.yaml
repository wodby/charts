apiVersion: v2
name: stateless
description: Boilerplate chart for stateless services
type: application
version: 0.2.1
annotations:
  artifacthub.io/changes: |
    - kind: fixed
      description: Omit RollingUpdate settings when the Recreate deployment strategy is selected
appVersion: "1.0.0"
dependencies:
  - name: common
    repository: oci://registry-1.docker.io/wodby
    version: 1.x.x
