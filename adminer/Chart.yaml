apiVersion: v2
name: adminer
description: Adminer chart for the Wodby Adminer image.
type: application
version: 0.3.2
appVersion: "6.1.0"
keywords:
  - adminer
  - database
dependencies:
  - name: common
    repository: oci://registry-1.docker.io/wodby
    version: 1.x.x
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: Pin the default image to wodby/adminer:6-r0
