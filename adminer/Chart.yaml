apiVersion: v2
name: adminer
description: Adminer chart for the Wodby Adminer image.
type: application
version: 0.3.1
appVersion: "6"
keywords:
  - adminer
  - database
dependencies:
  - name: common
    repository: oci://registry-1.docker.io/wodby
    version: 1.x.x
annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Drain service endpoints before Adminer receives its graceful stop signal
