apiVersion: v2
name: adminer
description: Adminer chart for the Wodby Adminer image.
type: application
version: 0.2.0
appVersion: "6"
keywords:
  - adminer
  - database
dependencies:
  - name: common
    repository: oci://registry-1.docker.io/wodby
    version: 1.x.x
annotations:
  artifacthub.io/changes: |
    - kind: security
      description: Update the default application image to Adminer 6.0.0 with upstream security fixes
